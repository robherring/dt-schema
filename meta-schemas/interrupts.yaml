id: "http://devicetree.org/schemas/interrupts.yaml#"
$schema: "http://json-schema.org/draft-06/schema#"

properties:
  minItems:
    type: integer
    minimum: 1
  maxItems:
    type: integer
    minimum: 1

required:
  - minItems
  - maxItems
